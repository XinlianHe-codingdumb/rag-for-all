# RAG FOR ALL 项目管理指南

这份文件写给项目负责人，而不是只写给程序员。它解释“东西放在哪里、现在能做什么、上线前还缺什么”。当你隔一段时间回来继续开发，先看这份文件，再看 `README.md`。

## 先用一句话理解项目

RAG FOR ALL 是一个可视化 RAG 实验室：用户上传文档，逐步查看文本解析、分块、向量化、检索、重排、Prompt 和最终回答；也可以改变参数并做 A/B 对比。

当前版本已经是一个可运行的单文档 MVP，不是只做了界面。它能解析 PDF、DOCX、TXT、Markdown，能保存文件与实验记录，也能在配置 OpenAI 服务端密钥后调用真实 Embeddings 和回答模型。

## 目录地图

| 位置 | 放什么 | 什么时候需要你关注 |
| --- | --- | --- |
| `app/` | 前端页面、交互逻辑、后端 API | 新增功能、改界面、改 RAG 步骤时主要在这里 |
| `app/rag-studio.tsx` | 核心产品界面；Overview、八个 RAG 步骤、A/B 对比、可视化 | 绝大多数产品和 UI 改动从这里开始 |
| `app/globals.css` | 全站视觉样式、排版、动画、响应式布局 | 改字体、颜色、间距、卡片、移动端布局 |
| `app/page.tsx` | 首页入口，加载 RAG Studio | 通常很少改 |
| `app/layout.tsx` | 页面标题、描述、分享卡片等全局元数据 | 改浏览器标题、SEO、分享预览时改这里 |
| `app/api/` | 服务器端接口，不会把 OpenAI 密钥发到浏览器 | 上传、删除、生成回答、嵌入、保存运行记录时改这里 |
| `app/api/events/route.ts` | 只接收白名单字段的匿名产品事件 | 新增需要统计的用户行为时先更新事件字典 |
| `app/api/admin/analytics/route.ts` | 站长数据与实时成本开关 API | 改仪表盘指标、Token 上限或紧急开关时关注 |
| `app/admin/` | 只有站长可看的 `/admin` 数据与成本面板 | 看各 Section 热度、漏斗、Token 用量时使用 |
| `app/lib/document-parser.ts` | PDF、DOCX、TXT、Markdown 变成可读文本 | 想支持 OCR、PPT、网页、表格时从这里扩展 |
| `app/lib/rag-engine.ts` | 分块、TF-IDF 向量、BM25、余弦相似度、混合检索、重排、Prompt 拼装 | 改检索算法、Chunk 策略、Rerank 逻辑时改这里 |
| `app/lib/rag-types.ts` | RAG 数据结构和配置类型 | 新增参数或某一步输出字段时同步修改 |
| `db/` | D1 / SQLite 的表结构与运行时绑定 | 新增需要长期保存的数据时改这里 |
| `db/schema.ts` | 文档、运行记录、限流、Token 用量、匿名事件、站点设置的正式定义 | 增加长期保存的数据时改这里 |
| `db/runtime.ts` | 读取 Cloudflare D1、R2、OpenAI 的服务端配置；确保表存在 | 环境变量或存储绑定变化时改这里 |
| `drizzle/` | 数据库迁移记录 | 每次正式修改表结构后，应生成一条新迁移，而不是手改线上库 |
| `public/` | 浏览器直接访问的静态资源：机器人插画、分享图、图标 | 新图片、Logo、静态素材放这里 |
| `tests/` | 自动测试与真实 PDF/DOCX 测试材料 | 改核心逻辑后运行 `npm test`；新增功能时补测试 |
| `docs/` | 产品和技术设计文档 | 写较稳定的产品决策、架构说明时放这里 |
| `examples/` | 参考代码，不是当前产品主流程 | 学习 D1 或试验功能时参考，不要误改成生产逻辑 |
| `worker/` | Cloudflare Worker 入口 | 平常不用动；需要底层 Worker 行为时才改 |
| `build/` | 构建工具的辅助代码 | 一般不碰 |
| `.openai/hosting.json` | 当前 Sites 项目的绑定：D1 叫 `DB`，R2 叫 `DOCUMENTS` | 不要随意删除或改项目 ID |
| `.env.local` | 你本机的 OpenAI 密钥；已被 Git 忽略 | 只能自己保留，绝不上传 GitHub 或发进聊天 |
| `.env.example` | 给新开发者看的环境变量模板，没有真实密钥 | 新增环境变量时同步更新 |
| `package.json` | 依赖、开发命令和 Node 版本要求 | 安装新库、增加脚本时改这里 |
| `README.md` | 项目入口说明 | 新成员或未来的你第一次打开项目时先看这里 |

以下文件夹是构建产物或本地缓存，不是源码，也不应提交：`node_modules/`、`dist/`、`.next/`、`.vinext/`、`.wrangler/`、`tmp/`。

## 现在的数据到底在哪里

```text
用户浏览器
  ├─ 7 天匿名会话 Cookie（不需要账号、姓名或邮箱）
  ├─ 本地读取、预览、分块和本地 TF-IDF 检索
  └─ 上传后 → API
                 ├─ R2：原始文件 + parsed.json
                 ├─ D1：文件元数据、实验摘要、匿名行为事件、用量与动态设置
                 └─ OpenAI：可选的真实 embedding 和回答调用
```

现在的持久化边界：

- D1 已保存：文档名称、类型、大小、页数、字符数、存储路径、创建时间；以及实验问题、配置和结果 JSON。
- R2 已保存：原始文件与解析后的文本 JSON。
- 删除按钮会删除这一份文档对应的 R2 文件、D1 文档记录与运行记录。
- **没有长期保存 embedding 向量。** 这在 MVP 阶段让系统简单、成本低；但文档量变大后，必须改成持久化向量索引。
- **没有用户账号，也不需要登录。** 每个浏览器使用随机匿名会话隔离文档与运行记录；清理浏览器站点数据后会获得新会话，旧数据仍按 7 天保留规则删除。
- 产品分析只保存页面/Section/步骤点击、漏斗状态和宽泛文件大小等白名单字段，不保存文档正文、文件名、问题、Prompt、回答、邮箱或原始 IP。

## 日常开发的最短流程

1. 打开项目目录，确认 Node.js 至少是 22.13。
2. 首次运行执行 `npm install`。
3. 创建 `.env.local`，填入自己的 OpenAI 密钥；没有密钥也能体验本地回退流程。
4. 运行 `npm run dev`，在浏览器打开本地地址。
5. 改完后运行 `npm test`。它会构建项目，并测试页面、PDF/DOCX 解析、分块、Top K 和 Rerank。
6. 看 `git status` 和 `git diff`，确认只提交了你想提交的文件。
7. 一次提交只解决一个清楚的问题，例如：`Improve retrieval history`。

常用命令：

```bash
npm run dev       # 本地开发
npm test          # 构建 + 自动测试
npm run lint      # 代码风格检查
npm run build     # 只做生产构建
git status        # 看哪些文件变了
git diff          # 看具体改了什么
```

## 正式给别人使用前：要做什么

下面不是“所有事情都必须今天做”，而是建议按优先级推进。

### 2026-08 公开 Beta 基础进度

目前已经具备公开免登录 Beta 的核心保护：文档和实验按匿名会话隔离；接口按会话与每日变化的 IP 哈希双重限流；调用模型前执行会话级和全站每日 Token 硬上限；站长可在 `/admin` 即时暂停模型；日志和分析不记录正文、完整问题、文件名、邮箱、原始 IP 或 API key。数据库迁移、安全响应头、Privacy/Terms、7 天文档保留和 90 天匿名分析保留也已完成。

当前代码已可进入公开 Beta 前的最后私有验收。正式把 Sites 切成 public 前，要确认 `/admin`、模型开关、Token 上限、上传到删除和线上环境变量都正确。自动告警、恶意文件扫描、删除审计和正式法律审查仍属于上线后的优先加固项。具体状态以 `docs/LAUNCH_READINESS.md` 为准。

### P0：不做就不该公开给陌生用户

1. **匿名会话与数据隔离**
   - 普通用户无需登录；服务端生成随机会话 ID，让 document、run 与 R2 object 只属于该匿名会话。
   - `/admin` 单独使用 Sites 的站长身份校验，普通访客不能进入。
   - 如果未来要做跨设备历史、团队共享或付费套餐，再增加正式账号、projects 与 memberships；当前工具型网站不需要。

2. **上传与滥用防护**
   - 已按匿名会话与每日加盐后的 IP 哈希对上传、回答、embedding、rerank 和历史写入设置限流，并有每日 Token 上限。
   - 已限制 MIME、20 MB 大小、解析后字符数和页数；公开前仍应增加文件签名校验和恶意文件扫描。
   - 对公开版本考虑恶意文件扫描和上传失败后的自动清理。
   - 后续补每个会话的每日文件数和总存储量上限。

3. **OpenAI 密钥与成本保护**
   - 正式环境的密钥只保存在 Sites/Cloudflare 的服务端 Secret，不放进前端、不放进 GitHub、不放进 `.env.example`。
   - 为每一次 embedding 与回答记录模型、输入/输出 token、延迟、估算成本和用户 ID。
   - `/admin` 已能动态调整会话级与全站每日 Token 上限，并能一键暂停付费模型调用。

4. **真实的隐私承诺**
   - 写 Privacy Policy、Terms of Use、删除与保留规则。
   - 明确告诉用户：哪些数据在浏览器、哪些在你的 R2/D1、哪些会发送给模型提供商。
   - 增加数据保留期限，例如未登录临时文件 24 小时自动删除、已登录项目可由用户主动删除。
   - 删除要有重试与审计记录，确保 R2、D1、未来的向量库都删除成功。

### P1：产品可用性和可运营性

5. **结构化日志、错误监控与告警**
   - 现在没有专门的产品级日志平台。上线前接入 Cloudflare Workers Logs + 错误监控服务（例如 Sentry）。
   - 每个请求记录：`requestId`、用户/项目匿名 ID、接口名、状态码、耗时、文件大小、模型名、token、成本、错误码。
   - 不记录：文档正文、完整 Prompt、API key、用户敏感内容。调试文本应明确脱敏并短期保留。
   - 设置告警：错误率升高、OpenAI 失败、请求延迟、R2/D1 错误、日成本异常。

6. **异步任务和可靠状态**
   - 目前上传、解析和部分计算会跟随一次请求完成。大文件、多文件或多人并发时，应该改用任务队列。
   - 可使用 Cloudflare Queues / Workers：上传成功后创建 job，前端显示 `queued → parsing → embedding → ready / failed`。
   - 每个 job 必须可重试、幂等（重复点击不会生成两份数据）、可取消。

7. **持久向量检索**
   - 现在 embedding 是临时的，适合学习和单文档演示。
   - 当有“多文档、历史项目、反复提问”需求时，引入向量库：Cloudflare Vectorize、Postgres + pgvector、Pinecone 或 Weaviate。
   - 向量记录至少包含：`chunkId`、`projectId`、`documentId`、embedding 模型版本、chunk 文本/指针、页码和创建时间。
   - 先做混合检索（关键词 + 向量）并保留当前可视化；不要为了“上向量库”丢掉可解释性。

8. **数据库演进和备份**
   - 现在只有 `documents` 与 `pipeline_runs` 两张核心表。加入用户/项目后，应通过 Drizzle migration 管理变化。
   - 生产库变更先在测试环境验证，再部署 migration；不要靠运行时 `CREATE TABLE IF NOT EXISTS` 作为长期迁移策略。
   - 制定 D1 导出备份、R2 版本/生命周期策略与恢复演练。

9. **产品分析，但不偷窥用户内容**
   - 已用自建事件表记录匿名访问、分流选择、卡片阅读、Section/步骤点击、上传、运行、回答、A/B 和引用查看。
   - `/admin` 显示 30 天匿名会话数、学习漏斗、Section 热度、14 天趋势和当日 Token 用量。
   - 新事件必须进入后端白名单；文件名、问题、Prompt、回答和正文即使被前端误传也不会保存。

### P2：质量、发布和商业化

10. **测试体系扩展**
    - 已有单元/集成测试；再补浏览器端到端测试：上传 → 跑流程 → 提问 → 删除。
    - 加真实失败用例：扫描 PDF、损坏 DOCX、超大文件、OpenAI 超时、R2/D1 不可用、并发删除。
    - 加可访问性检查：键盘操作、焦点、色彩对比、屏幕阅读器标签。

11. **环境与发布流程**
    - 保留三个环境：本地开发、Staging 测试、Production 正式站。
    - GitHub 分支建议：`main` 放可发布版本，`codex/*` 或 `feature/*` 做功能；用 Pull Request 审核后合并。
    - 每次公开发布应有版本说明、回滚方案、数据库迁移记录。
    - 绑定正式域名、配置 HTTPS、基础安全响应头和状态页。

12. **支付与套餐（真有用户后再做）**
    - 先设置免费额度：例如文档数、文件大小、每日提问数、每月 token 预算。
    - 再接 Stripe 等支付；套餐与配额必须在服务端检查，不能只藏在前端。
    - 对团队版增加成员、项目共享、权限、审计日志与用量账单。

## 推荐的上线路线

| 阶段 | 目标 | 必做项 |
| --- | --- | --- |
| 当前 | 公开前私有验收 | 检查 `/admin`、限流、成本开关、隐私说明、上传到删除闭环 |
| 公开 Beta | 陌生用户免登录直接使用 | 观察漏斗与成本、手动巡检日志、快速修复真实问题 |
| 稳定公开版 | 扩大流量 | 自动告警、恶意文件扫描、可恢复删除、Staging、备份、E2E 测试 |
| 商业化 | 有付费/团队用户 | 支付、配额、团队权限、审计、SLA、运营仪表盘 |

## 现在最值得先做的 5 件事

1. 私有线上环境完整跑一次 Overview → 上传 → Answer → Compare → 删除，并查看 `/admin` 是否出现对应漏斗。
2. 设置保守的站点 Token 日上限，实际测试暂停/恢复模型开关。
3. 公开后先观察 5–10 位目标用户，重点看他们从 Overview 到 Answer 在哪里离开。
4. 接入自动错误和成本告警；当前仪表盘需要站长主动查看。
5. 多文档与重复查询需求真正出现后，再决定持久向量库方案。

## 你可以怎样管理我后续的开发

每次告诉我四件事就够了：

1. **要解决谁的问题**：例如“完全不懂 RAG 的学生不知道 Rerank 的意义”。
2. **用户做完应看到什么**：例如“切换开关后，Rerank 前后结果必须并排显示”。
3. **是否涉及真实用户数据或付费**：这会决定我是否需要先做安全与数据库设计。
4. **是否要上线**：只说“本地改”我就不发布；说“上线”我会测试、提交并更新私有站点。

如果你只想提一个模糊想法也没关系，例如“我想让 Retrieve 更像一个游戏”，我会先把它翻译成可执行的产品任务，再开始做。
